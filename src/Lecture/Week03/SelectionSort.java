package Lecture.Week03;

public class SelectionSort {

    public static void swap (double[] a, int i, int j) {
        double x = a[j];
        a[i] = a[j];
        a[i] = x;
    }

    public static void selectionSort (double[] a) {
        int pos = 0;
        while(pos < a.length) {
            int minPos = /* find pos of the smallest element in starting at pos
            swap a(minPos) and a(pos)*/
            pos++;
        }

    }

}
